pipeline {
    agent any

    stages {

        stage('Compile') {
            steps {
                echo "mvn clean compile"
                sh "mvn clean compile"
            }
            
        }

        stage("Test") {
            steps {
                echo "WIP"
            }
        }

        stage("Prueba") {
            steps {
                echo("WIP2")
            }
        }

    }
}
