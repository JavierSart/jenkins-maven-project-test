pipeline {
    agent any

    stages {

        stage('Compile') {
            steps {
                echo "mvn clean compile"
                bat "mvn clean compile"
            }
            
        }

        stage("Test") {
            steps {
                echo "WIP"
            }
        }

        stage("Prueba") {
            steps {
                echo("WIP2")
            }
        }

    }
}
