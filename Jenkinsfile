pipeline {
    agent any

    stages {

        stage('test') {
            steps {
                echo "mvn clean compile"
                sh "mvn clean compile"
            }
            
        }

        stage("build application") {
            steps {
                echo "WIP"
            }
        }

        stage("Create docker image") {
            steps {
                echo("WIP2")
            }
        }

    }
}
